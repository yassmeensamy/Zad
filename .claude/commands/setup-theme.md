Set up the full theme system for a new Flutter app following EramDoctor conventions. Read the existing theme files in `packages/shared/lib/theme/` and `packages/shared/lib/extensions/theme_context_extensions.dart` first, then configure the target app to use the shared theme correctly.

Target: $ARGUMENTS
